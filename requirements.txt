# pymodbus's call signature for reading registers (the `slave=` keyword argument in particular)
# has changed across major versions, so this is pinned narrower than this repo's siblings.
pymodbus>=3.14.0,<3.15.0
requests~=2.34
paho-mqtt~=2.1
psycopg[binary]~=3.3
